# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (any workspace level)
node_modules/
.pnp
.pnp.*

# build outputs (any workspace level)
dist/
/build
/out/
.next/
.turbo/
*.tsbuildinfo
next-env.d.ts

# testing
/coverage

# env files (but keep the committed templates)
.env*
!.env.example
!**/.env.example

# misc
.DS_Store
*.pem
.vercel

# debug logs
npm-debug.log*
yarn-debug.log*
*-debug.log*

# data files (downloaded locally, not versioned)
puzzles.csv
puzzles.csv.zst

# personal planning docs (solo dev — not versioned)
docs

# Claude Code local settings + worktrees (never commit — worktrees are full repo copies)
.claude/
CLAUDE.md
