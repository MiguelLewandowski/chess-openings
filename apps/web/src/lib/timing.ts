// Timing for the training session, in milliseconds. Single source of truth so
// the cadence isn't scattered across the store and components.
export const OPPONENT_THINK_MS = 300 // pause before the opponent's reply appears
export const AUTO_ADVANCE_MS = 600 // delay before auto-playing the next reply (blunder mode)
export const NEXT_PUZZLE_MS = 2000 // delay before moving to the next puzzle
